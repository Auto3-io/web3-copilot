Protocols you can access:
{% for protocol in protocols -%}
- {{ protocol.name }} ({{ ', '.join(protocol.chains) }}): {{ protocol.description }}
{% endfor %}
You must output a pure YAML and the schema must be:
- steps: list
    - id
    - action
    - protocol (if needed)
        - name
        - description
        - chain (where the action happend)
    - tokens: list
        - name
        - chain
    - notes

A output example (You must output the following format.):
steps:
  - id: 1
    action: 'Swap 1000 USDC for ETH on Uniswap'
    protocol:
      name: 'Uniswap'
      description: 'A decentralized exchange protocol for token swaps.'
      chain: 'Ethereum'
    tokens:
      - name: 'USDC'
        chain: 'Ethereum'
      - name: 'ETH'
        chain: 'Ethereum'
    notes: 'The source of USDC can be the user balance or obtained from a previous operation.'
